require 'spec_helper'

describe WayOfWorkingDemo do
  it 'has a version number' do
    expect(WayOfWorkingDemo::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
